import { ToMediaUrlPipe } from './to-media-url.pipe';

describe('ToMediaUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ToMediaUrlPipe();
    expect(pipe).toBeTruthy();
  });
});


  